#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers
 * @a: First integer
 * @b: Second integer
 *
 * Return: Product of two integers
 */
int mul(int a, int b)
{
	int result = (a * b);
	return (result);
}
